version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: creditai
      POSTGRES_PASSWORD: creditai
      POSTGRES_DB: creditai
    ports: ["5432:5432"]
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
